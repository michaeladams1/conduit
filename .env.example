# Copy this file to .env for local testing.
# On Railway, set these as Variables in the project settings instead —
# you do not upload .env there.

OPENAI_API_KEY=sk-...

# Required after you run build-vector-store.js once (see README):
OPENAI_VECTOR_STORE_ID=vs_...

# Optional access control -- comma-separated list of allowed caller
# numbers, E.164 format (e.g. +13462149215,+15125551234). Leave unset
# or empty to allow anyone to call/text.
# ALLOWED_PHONE_NUMBERS=

# Optional overrides (defaults shown):
# REALTIME_MODEL=gpt-realtime
# SMS_MODEL=gpt-5.4-mini
# REALTIME_VOICE=alloy
# PORT=3000
